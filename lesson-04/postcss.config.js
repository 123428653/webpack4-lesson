module.exports = {
	plugins: {
		// 编辑目标浏览器：使用package.json中的“browserslist”字段，
		// 或者使用根目录下的.browserslistrc配置文件
    autoprefixer: {
    	// browsers: ['> 1%', 'last 2 version', 'not ie <= 8']
    }
  }
}